# Optional: Configure Postgres DB. If left empty, SQLite fallback is used.
# DATABASE_URL=postgresql://user:password@localhost:5432/pralaydrishti

# Required for AI processing. If empty, a Demo mock AI is used.
GEMINI_API_KEY=
